# Vanilla reference files

This directory will contain fresh, unmodified configuration files downloaded from the current Nitrado DayZ PlayStation server.

These files are reference snapshots and must not be edited directly.

They will be used to:

- Compare legacy files against the current vanilla configuration
- Identify confirmed custom changes
- Detect outdated classnames and settings
- Provide a clean rollback reference
